export enum ErrorMessage{
  UntouchedAuthentificationFields = "All fields are required",
  InvalidAuthentificationFields = "All fields have to contain valid input",
  StatusZeroRequest = "Unknown server error",
}
